// 4_함수2 - 인라인 함수 - 32page
int add1(int a, int b)
{
	return a + b;
}
inline int add2(int a, int b)
{
	return a + b;
}
int main()
{
	int n1 = add1(1, 2); // 호출. add1 함수로 이동
	int n2 = add2(1, 2); // 치환, add2 의 기계어 코드를 이부분에 치환해달라.
}
// 인라인 함수
// 장점 : 빠르다.
// 단점 : 기계어 코드가 여러번 치환되면 실행파일이 커질수 있다(?)

// 인라인 함수를 컴파일시 최적화 옵션이나 별도의 인라인 치환 옵션을
// 지정해야만 적용됩니다.

// cl 컴파일러의 "/Ob1" 은 인라인을 적용해달라는 옵션입니다.
//              "/O2" 는 최적화 옵션

// 2:00 에 이어집니다.